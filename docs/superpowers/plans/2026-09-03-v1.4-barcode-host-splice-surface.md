# Barcode v1.4 — Host Splice Surface

**Date:** 2026-09-03  
**Branch:** `feat/barcode-management-v1`  
**Status:** Catalog only — do not splice in this file's task  
**Question:** If we splice Barcode into the host today without merging `main`, what remains?

This is the remaining **host integration** work. It is not a dependency leak list. Gate E forbidden trees are absent from `git diff --name-only main...HEAD`.

Do **not** edit `vite.config.js`, `SystemModuleCatalog.php`, or `CommerceServiceProvider.php` from the extraction-readiness review. Listing them is the point.

---

## Surface

| Surface | Already on branch? | Required for host boot? | Leak or integration? |
|---------|--------------------|-------------------------|----------------------|
| `composer.json` — PSR-4 `Commerce\Barcode` → `modules/Barcode/src/` | Yes (branch hunk vs `main`) | Yes — class autoload | Integration |
| `composer.json` — `commerce/barcode` `@dev` in `require` | No | Yes — path-repo package install / discover | Integration |
| `composer.json` / lockfile — root `picqer/php-barcode-generator` | No (`composer.lock` not on branch; parent vendor may already have it) | Yes for `BarcodeLabelRenderer` on a clean host install | Integration |
| `modules/Barcode/composer.json` — `picqer/php-barcode-generator` | Yes | Yes (module package) | Integration |
| `modules/Barcode/composer.json` — `dompdf/dompdf` | No (PrintService still `use Dompdf\Dompdf`; manifest said add) | Yes for PDF download on a clean host install | Integration |
| `SystemModuleCatalog` — `barcode` row | No | Yes — module manager enablement | Integration (do not splice here) |
| `BarcodeServiceProvider` host register vs `module.json` `providers` | Module declares `Commerce\Barcode\BarcodeServiceProvider`; host `bootstrap/providers.php` does not list it | Yes — routes/views/lang/config boot | Integration |
| `CommerceServiceProvider` — `mergeConfigFrom` core `barcode.php` | No (module provider already `mergeConfigFrom` module `config/barcode.php`) | Core bind path: yes for kernel barcode config; module merge already covers module boot | Integration (do not splice here) |
| `CommerceServiceProvider` — bind `BarcodeValueGeneratorInterface` → `BarcodeValueGenerator` + four strategies | No (C2 tests bind it in `BarcodeFeatureTestCase`) | Yes for Center generate on host | Integration (do not splice here) |
| `vite.config.js` — `resources/css/barcode.css`, `resources/js/barcode/index.js`, `resources/js/barcode/history.js` | No (asset files exist on branch; Vite entries do not) | Yes for Center / history JS+CSS on host | Integration (do not splice here) |
| Permissions — `module.json` four codes (`barcode.print`, `barcode.history.view`, `barcode.template.manage`, `barcode.history.reprint`) | Yes | Yes — IamSeeder already scans `modules/*/module.json` | Integration (seed on host; no IamSeeder edit) |
| Menu — `config/admin.php` Barcode Center + `barcode.print` | Yes (already on `main`) | Yes after routes boot | Already on `main` — not a splice |
| Routes — `modules/Barcode/routes/web.php` | Yes | Yes — loaded only when `BarcodeServiceProvider` boots | Integration |
| Translations — `barcode::*` module lang | Yes (`modules/Barcode/resources/lang/{en,th}/admin.php`) | Yes after provider `loadTranslationsFrom` | Integration |
| Translations — `resources/lang/{en,th}/nav.php` `barcode_center` | Yes (already on `main`) | Yes for nav label | Already on `main` — not a splice |

---

## Not leaks

- `BarcodeOwnerResolver::resolve(?Product)` method argument (ADR-002/003). Constructor is `SettingQueryServiceInterface` only.
- `BarcodeWorkspaceService` Seller Eloquent query behind `class_exists` (not a Marketplace `*Service`).
- `BarcodeProductSearchService` Eloquent read of Product / Variant / ProductMedia (ADR-003). Constructor is OwnerResolver + ImageResolver.
- Missing Vite / catalog / host generator bind — host splice hunks from the extraction manifest, not Product/Inventory/Scanner adapters.

---

## Gate E (this branch vs `main`)

`git diff --name-only main...HEAD` is Copy + Create + `composer.json` PSR-4 only. Forbidden trees are absent:

```text
modules/Product/**
modules/Inventory/**
modules/WarehouseScanner/**
modules/Wishlist/**
modules/Catalog/**
modules/Pos/**
modules/Cart/**
modules/Settings/**
modules/Customers/**
modules/Reports/**
resources/views/components/storefront/**
resources/css/pos.css
resources/css/scanner.css
resources/js/pos/**
resources/js/scanner/**
SiteIdentityServiceInterface
```

---

## Next (not this task)

Splice allowlist hunks only, on a later host-wiring commit:

```text
composer.json / composer.lock   commerce/barcode @dev; picqer; regenerate lock
modules/Barcode/composer.json   add dompdf
vite.config.js                  three barcode entries; never pos.css / scanner.css
SystemModuleCatalog.php         one barcode row
CommerceServiceProvider.php     mergeConfigFrom core barcode.php; bind generator
```
