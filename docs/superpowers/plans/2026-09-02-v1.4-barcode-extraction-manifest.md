# WS-001 Extraction Manifest

**Date:** 2026-09-02  
**Status:** Locked (Phase 1 not started)  
**Base:** `main` @ `bbd2b9e` (or later docs-only commits on this line)  
**Archive:** `feat/barcode-stock-recovery` @ `84e905c`  
**Branch (when started):** `feat/barcode-management-v1` from current `main`

This is the **final Gate E allowlist**. Boundary, ADR-001, and ADR-002 stay in `2026-09-02-v1.4-barcode-management-boundary.md`. This file answers only: **which paths may appear on the extraction branch.**

```text
84e905c = Recovery Archive  ≠  Release Candidate
v1.4.0  = this allowlist + splice hunks only
Phase 1 = NOT STARTED
```

After the first copy, run:

```bash
git diff --name-only main...HEAD
```

Every path must match **Copy**, **Create**, or **Splice** below. Anything else is a boundary break — stop and revert.

---

## Copy from `84e905c` (path-by-path)

Inventory on the archive: **59** module + **10** JS + **1** CSS + **4** tests + **9** kernel files.

```text
modules/Barcode/**
resources/js/barcode/**
resources/css/barcode.css
tests/Feature/Barcode/**
tests/Unit/Barcode/**
```

Exact tests:

```text
tests/Feature/Barcode/BarcodeCenterAdminTest.php
tests/Unit/Barcode/BarcodeLabelStyleTest.php
tests/Unit/Barcode/BarcodeQueueArchitectureTest.php
tests/Unit/Barcode/NumericSequenceGeneratorTest.php
```

Exact JS:

```text
resources/js/barcode/index.js
resources/js/barcode/history.js
resources/js/barcode/label-style.js
resources/js/barcode/manual.js
resources/js/barcode/preview.js
resources/js/barcode/queue.js
resources/js/barcode/queue-item.js
resources/js/barcode/queue-validator.js
resources/js/barcode/search.js
resources/js/barcode/sequential-barcode.js
```

Exact kernel (new files on `main`; do not copy surrounding host files):

```text
packages/commerce/contracts/src/Barcode/BarcodeValueGeneratorInterface.php
packages/commerce/contracts/src/Barcode/BarcodeValueGeneratorStrategyInterface.php
packages/commerce/core/config/barcode.php
packages/commerce/core/src/Barcode/BarcodeValueGenerator.php
packages/commerce/core/src/Barcode/NumericSequenceGenerator.php
packages/commerce/core/src/Barcode/Strategies/PrefixBarcodeStrategy.php
packages/commerce/core/src/Barcode/Strategies/RandomBarcodeStrategy.php
packages/commerce/core/src/Barcode/Strategies/SequentialBarcodeStrategy.php
packages/commerce/core/src/Barcode/Strategies/TimestampBarcodeStrategy.php
```

Composer package: add `picqer/php-barcode-generator` on `main`. Do **not** copy `composer.lock` from `84e905c`.

---

## Create on the branch (not in the archive)

ADR-001 / ADR-003. New files under Barcode only:

```text
modules/Barcode/src/Services/BarcodeImageResolver.php
modules/Barcode/src/DTO/BarcodeSearchResult.php
tests/Feature/Barcode/BarcodePrintJobTest.php
modules/Barcode/database/migrations/2026_09_02_120000_add_preset_and_visibility_to_barcode_templates_table.php
```

```text
BarcodeProductSearchService
  -> BarcodeSearchResult
  -> BarcodeImageResolver::resolve(?string $mediaUuid): ?string
       -> MediaQueryServiceInterface::getUrl()   (already on main; no preload())
```

Do **not** copy `modules/Product/src/Services/ProductImageResolver.php`.
Do **not** add `preload()` to the Media contract.

---

## Adapt after copy (same paths, different internals)

These two files are copied, then rewritten to match the ADRs. Byte-for-byte archive versions are **not** the v1.4 shape.

```text
modules/Barcode/src/Services/BarcodeProductSearchService.php   # ADR-001/003: DTO + BarcodeImageResolver
modules/Barcode/src/Services/BarcodeOwnerResolver.php          # ADR-002: Seller -> store.name -> app.name -> Store
modules/Barcode/src/Services/BarcodePrintJobService.php        # ADR-004: queued → printed|failed
modules/Barcode/src/Http/Controllers/Admin/HistoryController.php  # ADR-004: reprint = re-render same job
modules/Barcode/src/Services/BarcodeQueueItemNormalizer.php    # ADR-004: no live owner on stored lines
modules/Barcode/composer.json                                  # ADR-004: add dompdf (missing on main)
modules/Barcode/src/Services/BarcodeTemplateService.php        # ADR-006: four presets
modules/Barcode/config/barcode.php                             # ADR-006: a4 + thermal catalog
modules/Barcode/resources/views/admin/templates/_form.blade.php  # ADR-006: preset + overrides
modules/Barcode/src/Services/BarcodePrintService.php           # ADR-006: honor show_*
tests/Feature/Barcode/BarcodeCenterAdminTest.php               # ADR-003/005: operator/admin/403; rewrite reprint JSON tests
tests/Unit/Barcode/BarcodeQueueArchitectureTest.php            # ADR-002/005: drop SiteIdentity mock
```

Owner fallback (ADR-002): never throw.

```text
Seller.name
  -> SettingQueryServiceInterface::get('store.name')
  -> config('app.name')
  -> 'Store'
```

Do **not** import `SiteIdentityServiceInterface`.

---

## Splice (hunks only — never replace the file)

R4 + Module Management. Take barcode lines into **current `main`**. Do not take archive versions of these files (they also contain Elasticsearch, ChannelContext, Footer, storefront, etc.).

| File | Allowed hunk |
|------|----------------|
| `composer.json` | `commerce/barcode` `@dev`; PSR-4 `Commerce\\Barcode\\`; `picqer/php-barcode-generator`. Path repo `modules/*` already covers the module. |
| `composer.lock` | Regenerated on `main` after `composer require`. Not copied from `84e905c`. |
| `vite.config.js` | Add `resources/css/barcode.css`, `resources/js/barcode/index.js`, `resources/js/barcode/history.js`. Do not add `pos.css` / `scanner.css`. |
| `packages/commerce/core/src/CommerceServiceProvider.php` | `mergeConfigFrom(.../config/barcode.php, 'barcode')`; bind `BarcodeValueGeneratorInterface` → `BarcodeValueGenerator` with the four strategies. Keep v1.1 `FeatureService` and v1.2 middleware. |
| `packages/commerce/core/src/Modules/SystemModuleCatalog.php` | One `barcode` catalog row. File does not exist on `84e905c` — write the row on `main`'s catalog. |

IAM permissions come from `modules/Barcode/module.json` via existing `IamSeeder` scan. Do **not** splice `IamServiceProvider` or copy `DatabaseSeeder` from the archive.

---

## Already on `main` — do not copy, do not overwrite

R2 (nav drift) is an orphan link, not a missing file.

```text
config/admin.php                          # already has admin.barcode.index + barcode.print
resources/lang/en/nav.php                 # barcode_center
resources/lang/th/nav.php                 # barcode_center
package.json                              # jsbarcode already present
```

After the module boots, the existing nav target is valid. Do not replace these with archive copies.

---

## Forbidden (Gate E fail)

If any of these appear in `git diff --name-only main...HEAD`, stop:

```text
modules/Product/**
modules/Product/src/Services/ProductImageResolver.php
modules/Inventory/**
modules/WarehouseScanner/**
modules/Wishlist/**
modules/Catalog/**
modules/Pos/**
modules/Cart/**
modules/Settings/**
modules/Customers/**
modules/Reports/**
resources/views/components/storefront/**
resources/css/pos.css
resources/css/scanner.css
resources/js/pos/**
resources/js/scanner/**
SiteIdentityServiceInterface (any path)
```

Also forbidden as a method, even if the resulting tree looks clean:

```bash
git merge feat/barcode-stock-recovery
git checkout 84e905c -- .
```

---

## Gate E expected shape

Phase 1 (copy only) should look like:

```text
modules/Barcode/**
resources/js/barcode/**
resources/css/barcode.css
tests/Feature/Barcode/**
tests/Unit/Barcode/**
packages/commerce/contracts/src/Barcode/**
packages/commerce/core/src/Barcode/**
packages/commerce/core/config/barcode.php
```

Later wiring commits may add only:

```text
composer.json
composer.lock
vite.config.js
packages/commerce/core/src/CommerceServiceProvider.php
packages/commerce/core/src/Modules/SystemModuleCatalog.php
```

plus the new `BarcodeImageResolver.php` under `modules/Barcode/**`.

Zero files under `resources/views/components/storefront`.
Zero Product / Inventory / POS / Scanner / Wishlist trees from `84e905c`.
